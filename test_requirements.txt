# Test requirements for sz_search_flask-v4
# These packages are needed to run the test suite

# Core testing framework (part of Python standard library)
# unittest - no additional installation needed

# For mocking HTTP requests in performance tests
requests==2.33.1

# Optional: Enhanced JSON performance (used by perftest if available)
orjson==3.11.8

# Optional: For test coverage reporting
coverage==7.13.5

# Optional: For test discovery and enhanced reporting
pytest==9.0.3
pytest-cov==7.1.0

# Flask testing utilities (Flask is already required by main application)
flask[testing]==3.1.3